def count_down(start):
    """ Count down from a number  """
    
    # call the count_down if the next
    # number is greater than 0
    next =  1 * next
    if next > 0 and next <= start:
        count_down(next)


count_down(3)